name: build

on:
  pull_request:
  push:
  schedule:
    # Prime the caches every Monday
    - cron: 0 1 * * MON

jobs:
  build:
    strategy:
      fail-fast: true
      matrix:
        os:
          - macos-latest
          - ubuntu-latest
          - windows-latest
        ocaml-compiler:
          - ocaml-base-compiler.5.0.0~rc1

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Use OCaml ${{ matrix.ocaml-compiler }}
        uses: ocaml/setup-ocaml@v2
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}
          opam-depext-flags: --with-test

      - run: opam install . --deps-only --with-test

      - run: opam exec -- dune build @install -p tiny_httpd_domain,tiny_httpd_domain_camlzip

      - run: opam exec -- dune build @src/runtest @examples/runtest @tests/runtest -p tiny_httpd_domain
        if: ${{ matrix.os == 'ubuntu-latest' }}

      - run: opam install tiny_httpd_domain

      - run: opam exec -- dune build @src/runtest @examples/runtest @tests/runtest -p tiny_httpd_domain_camlzip
        if: ${{ matrix.os == 'ubuntu-latest' }}
